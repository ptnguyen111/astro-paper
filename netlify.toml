[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

[context.staging]
  command = "npm run build"
  publish = "dist"

[context.main]
  command = "npm run build"
  publish = "dist"
